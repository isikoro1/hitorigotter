# hitorigotter web

ブラウザだけで動く、ひとりごと用のローカルメモアプリです。

## 使い方

`index.html` をブラウザで開くと起動します。投稿とアカウントは `localStorage` に保存されます。

## 機能

- 280文字の投稿
- `Ctrl + Enter` で投稿
- ジャンル別に使える複数アカウント
- タグ抽出
- 本文検索
- 投稿の固定
- 投稿の削除
- 下部バーでホーム、検索、アカウント、設定を切り替え
- 設定画面からJSONエクスポート / インポート

外部サーバーやアカウントは使いません。
